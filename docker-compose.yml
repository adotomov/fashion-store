services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fashion_store
      POSTGRES_PASSWORD: fashion_store
      POSTGRES_DB: fashion_store
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fashion_store"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
